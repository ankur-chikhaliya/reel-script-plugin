---
description: Research real hashtag reach/competition for a niche via live web search — source-backed, not guessed.
argument-hint: [niche or topic]
---

Niche/topic from user: $ARGUMENTS

Follow the `reel-hashtags` skill's process exactly (Step 1: get niche + platform, Step 2: run live web searches — never answer from memory, Step 3: return ranked hashtag table with mandatory Source column, Step 4: offer to swap the set into a `reel-script` caption). If `$ARGUMENTS` is empty, ask the user for the niche first.
