---
description: Write a complete Instagram Reel / TikTok / Short script — hooks, beats, CTA, caption, voiceover.
argument-hint: [topic or brief]
---

Topic/brief from user: $ARGUMENTS

Follow the `reel-script` skill's process exactly (Step 1: get essentials — topic, goal, language, length, and, only when relevant, link/handle + response method; Step 2: write script in its exact structure — hook options, script beats table, CTA, caption, voiceover-only block; Step 3: offer one iteration). If `$ARGUMENTS` is empty, ask the user for the topic first.
